# Skill Basic

Use when an agent needs to draft a message and write a local report.

Ask for approval before you send a Slack message or email.
Use apply_patch to create the report file.
Run npm test as a local command before finishing.
